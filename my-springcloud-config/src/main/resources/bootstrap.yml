server:
  port: 8030
spring:
  application:
    name: my-springcloud-config
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/011010110/my-springcloud-demo.git
#          username: 582966876@qq.com
#          password: lilinhua911
#          search-paths: /my-springcloud-config/src/main/resources/config
#      label: master
#    本地配置文件
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  profiles:
    active: native


eureka:
  instance:
    hostname: localhost
  #    preferIpAddress: true

  client:
    service-url:
      defaultZone: http://localhost:8000/eureka #注册中心地址
